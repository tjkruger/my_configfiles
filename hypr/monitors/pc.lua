-- PC monitor config
-- TODO: replace the empty output name with your actual monitor name(s) if needed.
hl.monitor({
    output = "",
    mode = "preferred",
    position = "auto",
    scale = "auto",
})