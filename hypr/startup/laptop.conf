# ============================
# LAPTOP STARTUP
# ============================

# Gnome keyring
# Only needed if not already started by the system/session
exec-once = /usr/bin/gnome-keyring-daemon --start --components=pkcs11,secrets,ssh,gpg && export $(/usr/bin/gnome-keyring-daemon --start)